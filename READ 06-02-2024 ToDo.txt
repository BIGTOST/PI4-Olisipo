ToDo:

1+2 - Acabar lógica de criar eventos naquele dia. Supostamente vai funcionar para ambas as reuniões e pedidos de ferias;

Para isto está criado o "screen.portal.meeting.events" e o "server.event.dart"

// logica, em teoria:
// adiciona um novo evento, coloca a hora de inicio e fim daquele dia,
// depois escolhe o Status, Event e EventType através de dropdowns (pode ser mudado),
// por fim adiciona tudo ao evento com o createEvent().

3 - noticias;

4 - ajuda de custos (screen.portal.expense.dart);